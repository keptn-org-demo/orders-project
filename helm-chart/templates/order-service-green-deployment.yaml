apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: order-service-green
  labels:
    app: {{ .Chart.Name }}-selector-order-service
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.orderServiceGreen.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-selector-order-service
        deployment: order-service-green
    spec:
      containers:
      - name: {{ .Values.orderServiceGreen.container.name }}
        image: "{{ .Values.orderServiceGreen.image.repository }}:{{ .Values.orderServiceGreen.image.tag }}"
        imagePullPolicy: {{ .Values.orderServiceGreen.image.pullPolicy }}
        env:
        - name: DT_TAGS
          value: "application={{ .Chart.Name }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        - name: DEPLOYMENT_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.labels['deployment']"
        - name: CONTAINER_IMAGE
          value: "{{ .Values.orderServiceGreen.image.repository }}:{{ .Values.orderServiceGreen.image.tag }}"
        - name: KEPTN_PROJECT
          value: "{{ .Chart.Name }}"
        - name: KEPTN_STAGE
          valueFrom:
            fieldRef:
              fieldPath: "metadata.namespace"
        - name: KEPTN_SERVICE
          value: "{{ .Values.orderServiceGreen.service.name }}"
        ports:
        - name: internalport
          containerPort: {{ .Values.orderServiceGreen.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        resources:
{{ toYaml .Values.resources | indent 12 }}
